#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <time.h>
#include <stdlib.h>

int main()
{
    int a = 2;
    int b = 3;

    printf("original : %d\n", a);
    printf("after xor 1회 : %d\n", a^b);
    printf("after xor 2회 : %d\n", (a^b)^b);

    return 0;
}
